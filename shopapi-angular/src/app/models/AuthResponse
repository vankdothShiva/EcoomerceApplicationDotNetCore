export interface AuthResponse {
  accessToken:  string;
  refreshToken: string;
  email:        string;
  fullName:     string;
  roles:        string[];
  expiresAt:    string;
  requiresMfa:  boolean;
  userId?:      string;
}